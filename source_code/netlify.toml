[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Même règle que public/_redirects (secours PG sans Mixed Content)
[[redirects]]
  from = "/api/*"
  to = "https://colweyz.ddns.net/api/:splat"
  status = 200
  force = true
